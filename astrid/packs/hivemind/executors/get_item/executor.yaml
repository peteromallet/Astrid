{
  "schema_version": 1,
  "id": "hivemind.get_item",
  "name": "Get Item",
  "kind": "external",
  "version": "2.0",
  "short_description": "Fetch a single full row from the Hivemind corpus by kind and id.",
  "description": "Retrieves an untruncated row from unified_feed by item_kind and item_id. For distillations, also fetches its distillation_cites. For messages and resources, also fetches distillations that cite the item.",
  "keywords": [
    "hivemind",
    "get",
    "fetch",
    "corpus",
    "distillation",
    "cite"
  ],
  "inputs": [
    {
      "name": "kind",
      "type": "string",
      "required": true,
      "description": "Item kind: message | resource | distillation."
    },
    {
      "name": "id",
      "type": "string",
      "required": true,
      "description": "Item id as a string; preserves exact Discord snowflake digits."
    }
  ],
  "outputs": [
    {
      "name": "item",
      "type": "file",
      "path_template": "{out}/item.json"
    }
  ],
  "command": {
    "argv": [
      "{python_exec}",
      "-m",
      "astrid.packs.hivemind.executors.get_item.run",
      "--kind",
      "{kind}",
      "--id",
      "{id}",
      "--out",
      "{out}/item.json"
    ]
  },
  "cache": {
    "mode": "none"
  },
  "isolation": {
    "mode": "subprocess",
    "network": true,
    "env_passthrough": [
      "HIVEMIND_API_URL"
    ]
  },
  "metadata": {
    "project_scope": "optional",
    "runtime_entrypoint": "main",
    "runtime_file": "run.py",
    "runtime_module": "astrid.packs.hivemind.executors.get_item.run"
    ,"network_policy": {"allowed_protocols": ["dns", "tcp"], "allowed_destinations": ["https://ujlwuvkrxlvoswwkerdf.supabase.co:443"], "allow_redirects": false, "broker": {"host_managed": true}}
  }
}
