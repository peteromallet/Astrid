{
  "schema_version": 1,
  "cache": {
    "mode": "none"
  },
  "command": {
    "argv": [
      "{python_exec}",
      "-m",
      "astrid.packs.generation.executors.generate_image.run",
      "--model",
      "{model}",
      "--mode",
      "{mode}",
      "--execution",
      "{execution}",
      "--out",
      "{out}"
    ]
  },
  "conditions": [
    {
      "input": "model",
      "kind": "requires_input"
    },
    {
      "input": "mode",
      "kind": "requires_input"
    },
    {
      "input": "execution",
      "kind": "requires_input"
    }
  ],
  "description": "Generate images from text prompts using local (vibecomfy), cloud (fal), or Codex image_generation backends. v2: model\u2192mode\u2192backend taxonomy with required --mode.",
  "graph": {
    "consumes": [
      "prompt_or_prompts_file",
      "image_ref"
    ],
    "provides": [
      "generated_images",
      "image_manifest"
    ]
  },
  "id": "generation.generate_image",
  "inputs": [
    {
      "description": "Generation mode: t2i (text-to-image), i2i (image-to-image), edit (instruction-guided edit). Required per SD-005.",
      "name": "mode",
      "required": true,
      "type": "string"
    },
    {
      "default": "",
      "description": "Text prompt for image generation (mutually exclusive with prompts_file).",
      "name": "prompt",
      "required": false,
      "type": "string",
      "artifact_type": "prompt"
    },
    {
      "default": "",
      "description": "JSON/JSONL file of prompts (mutually exclusive with prompt).",
      "name": "prompts_file",
      "required": false,
      "type": "file",
      "artifact_type": "prompt"
    },
    {
      "description": "Model ID from the generation model registry (e.g. 'flux-dev', 'z-image').",
      "name": "model",
      "required": true,
      "type": "string"
    },
    {
      "default": "",
      "description": "Reference image path or URL for i2i/edit modes.",
      "name": "image_ref",
      "required": false,
      "type": "string",
      "artifact_type": "image"
    },
    {
      "description": "Execution backend: 'local' (vibecomfy), 'cloud' (fal), or 'codex' (Codex image_generation).",
      "name": "execution",
      "required": true,
      "type": "string"
    },
    {
      "default": "1",
      "description": "Number of images to generate (sequential N=1 loop).",
      "name": "count",
      "required": false,
      "type": "integer"
    },
    {
      "default": "",
      "description": "Deterministic seed for reproducibility.",
      "name": "seed",
      "required": false,
      "type": "integer"
    },
    {
      "default": "",
      "description": "Negative prompt describing what to avoid.",
      "name": "negative_prompt",
      "required": false,
      "type": "string",
      "artifact_type": "prompt"
    },
    {
      "default": "",
      "description": "Output size (e.g. '1024x1024' or fal size preset).",
      "name": "size",
      "required": false,
      "type": "string"
    },
    {
      "default": "",
      "description": "Codex quality hint folded into the natural-language prompt: low, medium, high, or auto.",
      "name": "quality",
      "required": false,
      "type": "string"
    },
    {
      "default": "",
      "description": "Codex background hint folded into the natural-language prompt: transparent, opaque, or auto.",
      "name": "background",
      "required": false,
      "type": "string"
    },
    {
      "default": "300",
      "description": "Codex per-image timeout in seconds.",
      "name": "timeout",
      "required": false,
      "type": "integer"
    },
    {
      "default": "",
      "description": "Denoising strength for i2i mode (0.0-1.0).",
      "name": "strength",
      "required": false,
      "type": "number"
    },
    {
      "default": "",
      "description": "Classifier-free guidance scale.",
      "name": "guidance_scale",
      "required": false,
      "type": "number"
    },
    {
      "default": "",
      "description": "Number of sampling steps.",
      "name": "steps",
      "required": false,
      "type": "integer"
    },
    {
      "default": "",
      "description": "Upscale mode: 'factor' (multiply dimensions) or 'target' (target resolution).",
      "name": "upscale_mode",
      "required": false,
      "type": "string"
    },
    {
      "default": "",
      "description": "Upscale factor (1-10) when upscale_mode is 'factor'.",
      "name": "upscale_factor",
      "required": false,
      "type": "number"
    },
    {
      "default": "",
      "description": "Target resolution when upscale_mode is 'target': 720p, 1080p, 1440p, 2160p.",
      "name": "target_resolution",
      "required": false,
      "type": "string"
    },
    {
      "default": "",
      "description": "Noise scale (0-1) for the upscaler.",
      "name": "noise_scale",
      "required": false,
      "type": "number"
    },
    {
      "default": "",
      "description": "Comma-separated LoRA registry ids and/or path@scale specs (e.g. 'flux-realism,z-realgen-v2' or 'https://...@0.8').",
      "name": "loras",
      "required": false,
      "type": "string"
    },
    {
      "default": null,
      "description": "Closed shot-generation recipe preserving prompt bindings, ordered references, generator identity, and content hashes.",
      "name": "shot_generation_recipe",
      "required": false,
      "type": "json"
    }
  ],
  "isolation": {
    "mode": "subprocess",
    "network": true
  },
  "keywords": [
    "image",
    "generate",
    "flux",
    "fal",
    "codex",
    "gpt-image",
    "vibecomfy",
    "i2i",
    "t2i",
    "edit",
    "z-image",
    "qwen"
  ],
  "kind": "built_in",
  "metadata": {
    "output_result_manifest": true,
    "requires_timeline": false,
    "api_provider": "multi",
    "env": [
      "FAL_KEY"
    ],
    "runtime_entrypoint": "run_sdk",
    "runtime_entrypoint_note": "run_sdk is the in-process entrypoint returning a JSON-safe payload dict; subprocess / CLI compatibility remains via main() and the __main__ guard at the bottom of run.py.",
    "runtime_file": "run.py",
    "runtime_module": "astrid.packs.generation.executors.generate_image.run",
    "network_policy": {"allowed_protocols":["dns","tcp"],"allowed_destinations":["queue.fal.run:443","fal.run:443","storage.fal.run:443"],"allow_redirects":false,"broker":{"host_managed":true,"kind":"broker","enforced":true,"observable":true,"route":"declared-http-routes","wrapper":"astrid-generic-host"},"descendant_enforcement":{"kind":"broker","validated":true,"observable":true,"route":"declared-http-routes","wrapper":"astrid-generic-host"}}},
  "name": "Generate Image",
  "outputs": [
    {
      "description": "Generated image files identified by receipt entries.",
      "mode": "create_or_replace",
      "name": "generated_images",
      "type": "file",
      "artifact_type": "image"
    },
    {
      "description": "JSON manifest of the generation run.",
      "mode": "create_or_replace",
      "name": "image_manifest",
      "path_template": "{out}/manifest.json",
      "type": "file",
      "artifact_type": null
    }
  ],
  "scoped_configs": ["credentials.fal"],
  "short_description": "Generate images from text prompts via local, cloud, or Codex backends. v2: model→mode→backend.",
  "version": "2.0"
}
