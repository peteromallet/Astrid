# Publication and source provenance

Public tracked target: `peteromallet/Astrid`, base branch `main`, path
`docs/projects/runpod-managed-generation-20260921/`.
Host repository: https://github.com/peteromallet/Astrid.git.
Packet-host **base** SHA: `032f65bcd84bd6360f83c260aa902c3a70cc0575`.
Packet-host commit SHA: **not created**. This packet is currently new, uncommitted
content in the intended tracked path. No handoff branch, commit, push or merge
was performed; the user prohibited product repository pushes/merges.

The archive is a filesystem snapshot, identified by its external SHA-256. Its
manifest, source locks and control hashes make it usable independently of a
packet-host commit. Do not claim the base SHA contains these new packet files.
When separately authorized, stage only this explicit directory on the selected
publication branch, commit, publish without force, verify the remote ref, and
refresh the packet-host commit record without pretending it is a product
acceptance revision. No command in this packet auto-publishes.

Public source objects were retrieved anonymously into fresh directories.
[The retrieval report](evidence/source-fetch-report.json) records per-repository
PASS/unavailable outcomes. This proves fetchability and exact checkout identity
at packaging time, not product acceptance, GitHub branch currency or future
availability. Cached origin/main values in locks are labeled cached, not live
observations. No existing repository was fetched, checked out, reset or staged.

Project A current inspected tuple:

| Repo | Exact selected commit | Acceptance classification |
|---|---|---|
| Astrid | `032f65bcd84bd6360f83c260aa902c3a70cc0575` | Current committed candidate; recorded deterministic approval not yet frozen-SHA-bound |
| VibeComfy | `fba5567d4efb59e2ae5020b8ad0d3db11cf02910` | Current committed producer candidate; same qualification gap |
| runpod-lifecycle | `f6a94f52a7f9a5a83e83de46abbec8a9f86dfd77` | Current committed transport candidate; same qualification gap |

Historical baseline SHAs remain in the original source-state controls. The
VibeComfy engine closure records historical publication at Astrid
`8e639b69351e567b46124cd7547ae9dde8cadaa1`, explicitly with RunPod qualification
transferred to the successor. That historical closure is not full warm-engine
or current-program acceptance. Under the latest scope amendment, RunPod
qualification is now assigned to separate post-handoff gate L, not the current
successor projects. CPU implementation completion can be published as such when
separately authorized, with GPU_UNQUALIFIED recorded.

The inspected skills repository is
`https://github.com/peteromallet/poms-skills.git` at
`f1296034386486d65cf19876d5e5ebfb0547e7cf`, with modified/untracked skill controls.
Only explicitly selected skill documents are exported, each with original and
sanitized hash and tracked/dirty classification. No private skill installation
or machine configuration is shipped. Product and package declarations are
pinned or explicitly unresolved in the two lock files.

Publication audit covers packet bytes and fetched source identity. Repository
history is not bundled or newly published, and no full source-history secret
audit was attempted. No credential, environment file, private key, raw transcript,
model weight, cache or operational resource configuration is in the packet.
