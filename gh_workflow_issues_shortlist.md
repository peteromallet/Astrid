# GitHub Workflow-Issue Deep Dive — 10 ComfyUI-ecosystem repos, past 6 months

**329 issues processed** across: ComfyUI core, KJNodes, LTXVideo, WanVideoWrapper, GGUF,
workflow_templates (official), comfy-kitchen, comfy-aimdo, SageAttention, ComfyUI-Manager.

Classified: **GENERALIZABLE-WORKFLOW** 62 · **CORE BUG** 118 · **INFRASTRUCTURE** 80 · **NOT-GENERALIZABLE** 69

Solutions: **confirmed yes** 71 · partially 108 · no 150

## A. Generalizable workflow problems — CONFIRMED solutions

| # | repo | difficulty | problem | solution (confirmed) | issue |
|---|------|-----------|---------|---------------------|------- |
| 1 | ComfyUI | EASY | CLIPTextEncode fails with ValueError 'not enough values to unpack (expected 4, got 1)' (GGUF text-encoder path) on an outdated ComfyUI install. | OP (C1): 'The ComfyUI version was too old, but it's running fine after the update.' m8rr (C0) had pointed to the GGUF node. Upgrading ComfyUI resolves it. | [#15797](https://github.com/Comfy-Org/ComfyUI/issues/15797) |
| 2 | ComfyUI | EASY | H3 generated-video audio always has static/popping on RTX 4080 Super across ComfyUI 0.31/0.33 regardless of sampler, LoRA, precision, or attention choice. | OP (C2/C3): fixed by downgrading - the portable ComfyUI 0.30.0 build generates normal audio. (Regression in newer versions; no upstream fix identified.) | [#15614](https://github.com/Comfy-Org/ComfyUI/issues/15614) |
| 3 | ComfyUI | EASY | VAEDecodeTiled on MiniMax H3 video fails with 'to() received an invalid combination of arguments - got (NestedTensor)'. | Maintainer rattus128 (C3): the MiniMax VAE has built-in tiling (trained tile size, <8GB VRAM) - use the standard VAE Decode node, the tiled node is incompatible. LIQUIDMIND111 (C4) confirms standard decode works. | [#15274](https://github.com/Comfy-Org/ComfyUI/issues/15274) |
| 4 | ComfyUI | EASY | H3 audio/video VAE decode crashes (NestedTensor error in decode_tiled) on torch 2.9.1+cu128 (WSL and Windows); related triton 'Python.h not found' compile errors also seen. | OP (C5): updating to torch 2.11.0+cu128 fixed it on Windows. cushycrux (C6) fixed the triton compile variant via woct0rdho/triton-windows #8 (embedded-python notes). hum-ma (C8): don't use the tiled node - H3 VAE has built-in tiling (see #15274). | [#15246](https://github.com/Comfy-Org/ComfyUI/issues/15246) |
| 5 | ComfyUI-WanVideoWrapper | EASY | InfiniteTalk/MultiTalk crashes at multitalk.py:247 reshape (tensor size mismatch) after updating to ComfyUI 0.19.x; suspected off-by-one frame count. Anyone running 800x456 (non-16-divisible resolution) hits it. | OP confirmed root cause was resolution: 800x456 is not divisible by 16 (456/16=28.5), causing the audio cross-attention reshape mismatch; switching to 832x480 in ImageScaleByAspectRatio fixed it completely (OP comment 'rookie-thomas'). Corroborated by Booyaka101's clean repro on current main and maintainer (kijai) saying the example workflow runs unchanged. | [#1998](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/1998) |
| 6 | workflow_templates | EASY | gsl_starter_1_3 template requires an input image (car and landscape) that is not bundled with the template and has no note on where to obtain it, so the workflow is incomplete for every fresh user. | Maintainer (comfyui-wiki) stated the missing image is available at https://comfyui-template-manager.vercel.app/ (comment 2026-07-16 with screenshot). Definitive source for the asset; template itself still lacks a pointer. | [#1005](https://github.com/Comfy-Org/workflow_templates/issues/1005) |
| 7 | workflow_templates | EASY | image_hidream_o1_dev template produces a grey image (or pure noise for others, incl. fp8/mxfp8 variants; edit mode broken) even with default prompts and correct model placement. Root cause: the ImageScaleToTotalPixels node in the template is bypassed, corrupting the image path. | Commenter smithian123 (2026-05-19): un-bypassing the ImageScaleToTotalPixels node fixed the grey output. Affects anyone who runs the template as shipped since the node ships bypassed. | [#866](https://github.com/Comfy-Org/workflow_templates/issues/866) |
| 8 | ComfyUI-KJNodes | EASY | SageAttention (3.1.0) on Wan 2.2 video generation causes temporal flickering/flashing that disappears when switching back to default sdpa attention with all other parameters unchanged — quantized attention compromises temporal layers. | OP-confirmed (issue body): switch back to sdpa attention for wan2.2 video; flicker fully disappears. No maintainer reply, so treat as known trade-off of SageAttention quantization. | [#629](https://github.com/kijai/ComfyUI-KJNodes/issues/629) |
| 9 | ComfyUI-KJNodes | EASY | Two issues: (1) rounding 32 floors 1080→1056 instead of rounding up; (2) IndexError when crop is enabled with a mask attached — caused by Load Image passing a 64x64 placeholder mask when none was drawn/edited. | OP diagnosed and maintainer kijai (2026-04-02) confirmed: open the mask editor and save (even unchanged) to replace the 64x64 placeholder; maintainer promised a guard. Rounding stays floor-by-design — maintainer reluctant to change existing workflows' behavior. | [#586](https://github.com/kijai/ComfyUI-KJNodes/issues/586) |
| 10 | ComfyUI-LTXVideo | EASY | Loading state_dict for LTXAVModel fails with scale_shift_table size mismatches (5,2048 vs 8,2048 etc.) after downloading models from HuggingFace — checkpoint version vs node-pack/model-code version mismatch. | Maintainer (rluxemburg) states definitive cause: 'This is a checkpoint/code version mismatch. Please check whether you downloaded LTX-2 or LTX-2.3 model weights' — match weight version to the node pack. | [#502](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/502) |
| 11 | ComfyUI-WanVideoWrapper | HARD | WanVideo BlockSwap causes unbounded system-RAM growth on Windows (96GB filled by ~3rd run, pinned-memory related), and sageattn_3 + torch.compile either OOMs, degrades quality, or reduce-overhead fails (cudaMallocAsync pool conflict on Windows). Self-resolved with detailed evidence, 0 comments. | OP self-confirmed workarounds with logs: run ComfyUI with --disable-pinned-memory and replace the WanVideo BlockSwap node with the older 'WanVideo Enhanced BlockSwap' node — this stopped RAM growth and enabled 1920x1024x61-frame generation on a 5060 Ti 16GB. For sageattn_3: use the per_block_mean sageattn3 mode (or keep SageAttention 2.2 for first/last steps); torch.compile reduce-overhead is blocked by Windows cudaMallocAsync pool limits (not fixable via --disable-cuda-malloc). | [#1986](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/1986) |
| 12 | ComfyUI-KJNodes | MEDIUM | LTX-2.3 videos degraded to muddy, structureless output after dependency churn; intermittent 'ghost' recovery. Root cause: running without the required distill LoRA and with xformers installed (should not be used with these nodes). | Maintainer kijai (2026-06-13): output looks like wrong model or missing distill lora; uninstall xformers, use standard pytorch attention or SageAttention. OP confirmed quality returned after removing xformers, cleaning caches and re-attaching the LoRAs. | [#669](https://github.com/kijai/ComfyUI-KJNodes/issues/669) |
| 13 | ComfyUI-GGUF | MEDIUM | MiniMax-H3 GGUF (UNet + qwen3vl text encoder) unsupported by mainline nodes; users need a working loader/TE combination and correct workflow settings. | molbal's fork (github.com/molbal/ComfyUI-GGUF) loads MiniMax-H3 GGUFs incl. Unsloth quants — OP alish119 confirmed it 'works excellently' after fixing his own error (pixel count must be a multiple of 32) and using the 'Unet Loader (Dynamic VRAM)' node inside the comfy.org subgraph workflow (molbal comment + OP confirmation). | [#471](https://github.com/city96/ComfyUI-GGUF/issues/471) |

## B. Generalizable — partial/unconfirmed solutions

| # | repo | difficulty | problem | suggested fix (unconfirmed) | issue |
|---|------|-----------|---------|------------------------------|------- |
| 14 | ComfyUI | EASY | ComfyUI-llama-cpp_vlm node errors 'Image input detected, but the loaded model is not configured with a mmproj module' - the loaded GGUF lacks mmproj vision weights. | niknah (C0): error comes from the third-party ComfyUI-llama-cpp_vlm node - download the mmproj weights for the model or use an image-capable model. 808charlie (C1) points to MiniMax-H3-Prompt-Rewriter node as an alternative. No OP confirmation. | [#15859](https://github.com/Comfy-Org/ComfyUI/issues/15859) |
| 15 | ComfyUI | EASY | After the DynamicVRAM/AIMDO updates, models kept on a slow HDD are re-read from disk every run (~15 min vs ~10 s on RTX 4090); users need the old keep-in-VRAM behavior. | Maintainer rattus128 (C0) lists the existing flags: --disable-dynamic-vram, --high-vram, --high-ram, --fast-disk. alecz20 (C2) confirms --disable-dynamic-vram restores 10x speed on HDD, but core warns the flag will be removed soon. OP's bat edit didn't help and wasn't resolved. | [#15661](https://github.com/Comfy-Org/ComfyUI/issues/15661) |
| 16 | ComfyUI | EASY | GGUF Wan2.2 models on 5060 Ti 16GB offload aggressively to system RAM as resolution rises; disabling flags doesn't change it. | Maintainer comfyanonymous (C3): 'Stop using gguf' - use native model formats. rattus128 (C4): logs show legacy loader with GGUF, not aimdo; asked to retest with native models and no custom nodes. OP never retested; consistent guidance elsewhere is native formats + dynamic VRAM. | [#14447](https://github.com/Comfy-Org/ComfyUI/issues/14447) |
| 17 | ComfyUI | EASY | With dynamic VRAM, 2-pass Qwen-Image-Edit unloads all models between samplers and after the run (RAM flushed), doubling generation time vs --disable-dynamic-vram. | Workaround --disable-dynamic-vram confirmed working by Kozatiju (C5) and others, but rattus128 only reproduced/asked for logs and the flag is slated for removal; underlying reload behavior not fixed. | [#13139](https://github.com/Comfy-Org/ComfyUI/issues/13139) |
| 18 | ComfyUI | EASY | After an update, generations get 'lowvram patches' stacking each run and 60s -> 380-780s runtimes on a 4090/4060Ti (dynamic VRAM / smart memory era). | Multiple user-confirmed settings fixes: Kitaiwish (C4) 'turning off dynamic VRAM mode' restored speed; x3style (C11) enabling 'Disable smart memory management' in Settings->Server-Config restored 30-40s gens. rattus128 (C1): disable shared-memory fallback in NVIDIA control panel + manually tile the VAE decode. OP's own disable-dynamic-vram test didn't help; maintainer suggests native models over GGUF (C5). | [#12876](https://github.com/Comfy-Org/ComfyUI/issues/12876) |
| 19 | ComfyUI-WanVideoWrapper | EASY | I2V fails on ROCm 7.1/Windows with shifting tensor errors (shape invalid in unpatchify, 'Boolean value of Tensor'). A peer user with a near-identical stack shows the I2V code path works fine on ROCm, pointing to a config mistake (invalid frame count), not the AMD environment. | Peer user (zisisvangelis-gif) with the same PyTorch/ROCm/Wrapper stack completed >12 I2V generations and identified OP's num_frames:16 as invalid — Wan needs 4n+1 frames (17,21,...); suggested trying 17 plus a full working config (tiled_vae false, blocks_to_swap low). Not OP-confirmed; also measured blocks_to_swap 10 vs 20 = 8.7x speed difference. | [#2037](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/2037) |
| 20 | ComfyUI-WanVideoWrapper | EASY | Wan2.2 I2V A14B workflow with two-pass HIGH/LOW samplers crashes in WanVideoImageToVideoEncode: VAE expects 36 channels but input has 68 — classic wrong-VAE symptom (68 = 36+32 extra latents from end_image/extra channels path). | Commenter chenxluo gives the definitive cause: use the Wan2.1 VAE, not the Wan2.2 VAE from the ComfyUI repo, citing the identical issue #1281. Not OP-confirmed in thread. | [#2003](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/2003) |
| 21 | workflow_templates | EASY | Z-Image-Turbo Text to Image template wires ConditioningZeroOut into the negative KSampler input; at CFG above 1 this produces distorted output, while an empty CLIP Text Encode node gives cleaner results. | Commenter lin-bot23 says Z-Image-Turbo expects CFG=1 and takes no negative prompt - distortion comes from high CFG, not the node; use the Z-Image template if negative prompting is needed (https://github.com/Tongyi-MAI/Z-Image). OP (FrizzSoft) disputed, arguing empty CLIP Text Encode behaves better at CFG~1.5. No maintainer ruling or template change confirmed. | [#931](https://github.com/Comfy-Org/workflow_templates/issues/931) |
| 22 | workflow_templates | EASY | templates/api_google_nano_banana2_image_edit.json references node type GeminiNanoBanana2V2 which no longer exists on Comfy Cloud (only GeminiNanoBanana2), failing validation for everyone ('unknown node type(s)'), and its widgets_values are in the old V2 order so a mere rename still fails value_not_in_list checks. | Reporter (from comfy_cloud MCP smoke test) provided full diagnosis and two concrete fixes: republish the template against the current GeminiNanoBanana2 node schema (prompt, model, seed, aspect_ratio, resolution, response_modalities, thinking_level) or reinstate GeminiNanoBanana2V2 on cloud. No maintainer response; nothing confirmed fixed. | [#858](https://github.com/Comfy-Org/workflow_templates/issues/858) |
| 23 | workflow_templates | EASY | audio_ace_step_1_5_checkpoint template loads with missing core nodes (EmptyAceStep1.5LatentAudio, TextEncodeAceStepAudio1.5 — shipped with ComfyUI 0.11.1); ACE custom nodes also crash get_object_info because ACE-Step model dirs don't exist. A second user hit the same on every ACE 1.5 template with a Desktop build reporting no updates available. | Maintainer (comfyui-wiki) said 'Please update your comfyui first' — the missing nodes are core nodes added in ComfyUI 0.11.1. OP never confirmed; dhosein remains stuck because their Desktop install reports no update available. | [#536](https://github.com/Comfy-Org/workflow_templates/issues/536) |
| 24 | ComfyUI-LTXVideo | EASY | Generated videos ignore the input image and follow the text prompt instead (character consistency broken in i2v short-drama use). | Another user (ulissescappato, same symptom) suggests disabling the 'prompt_enhance' option so output follows the input image; not confirmed by OP. | [#518](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/518) |
| 25 | ComfyUI-LTXVideo | EASY | LTX 2.3 template's 'Models Links' node omits the required Gemma text encoder download and lists an unused 'abliterated' lora — anyone following the template's links hits a missing text encoder. | soonyigkka says the docs misplace the file (put gemma-3-12b-it-abliterated_lora_rank64_bf16.safetensors in text_encoders, with screenshot); OP disputes it's a text encoder (size check); unresolved — grab the Gemma text encoder separately from Comfy-Org/ltx-2. | [#489](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/489) |
| 26 | ComfyUI-GGUF | EASY | Request for JoyAI Image Edit GGUF support; the linked vantagewithai GGUF already carries 'wan' in its model name so existing nodes handle it. | m8rr comment: the vantagewithai JoyAI GGUF uses 'wan' in its model name and 'works without needing any code changes' — no OP confirmation in thread. | [#466](https://github.com/city96/ComfyUI-GGUF/issues/466) |
| 27 | workflow_templates | EASY | The flux2.dev template's subgraph conversion dropped/hid the multi-image input the older workflow had; maintainer admits 'maybe we do a bad conversion for this template'. | Maintainer (comfyui-wiki) posted a workaround video: use the flux.2 dev fp8 template instead and unpack the subgraph — the multi-image inputs are there. No OP confirmation. | [#457](https://github.com/Comfy-Org/workflow_templates/issues/457) |
| 28 | ComfyUI-LTXVideo | EASY | MathExpression node between Duration and EmptyLTXVLatentVideo fails with simpleeval.NameNotDefined: 'b' is not defined — the template node's 'b' input is unconnected. | alexandrezia (same error): expand the 'math expression' node and connect the 'Frame rate' node into input 'b'; no OP confirmation. | [#452](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/452) |
| 29 | ComfyUI-GGUF | EASY | UnetLoaderGGUF only outputs MODEL, so the selected GGUF filename can't flow into save-node metadata/filenames; request to add a STRING 'modelname' output. | roobbb posted a complete local subclass workaround (new UnetLoaderGGUFwithName class in nodes.py returning (model, unet_name)) and reports 'it works fine in my workflows' — not merged upstream and gets overwritten on update. | [#447](https://github.com/city96/ComfyUI-GGUF/issues/447) |
| 30 | ComfyUI-LTXVideo | EASY | Official LTX-2.3 T2V/I2V two-stage distilled workflow produces 'mosaic/flickering screen' artifacts despite up-to-date ComfyUI and node pack. | raffetazarius suggests using the v1.1 LTX-2.3 upscaler since v1.0 had a known issue; unconfirmed by OP. | [#444](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/444) |
| 31 | ComfyUI-LTXVideo | EASY | LTX 2.3: image-to-video template works but text-to-video template fails with 'AssertionError: Audio VAE config is required for audio VAE' — the working template uses KJ nodes/models and the broken one uses LTX nodes (mixed ecosystems); also blocks upscalers in I2V. | GlamoramaAttack gives the definitive cause: 'You're using either LTX models and nodes or KJ' models and nodes. You can't mix it' — stick to one ecosystem's models+nodes; no OP confirmation. | [#436](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/436) |
| 32 | ComfyUI-LTXVideo | EASY | Long (30s) i2v generations show severe artifacts in the last few frames of the video. | Gilles-61 suggests deleting all text in the negative prompt box inside the subgraph and regenerating; unconfirmed by OP. | [#434](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/434) |
| 33 | workflow_templates | EASY | Freshly-installed template fails at CLIPLoader: 'size mismatch for Llama2 state_dict' (Qwen3-shaped 2560-hidden weights loaded into a Llama arch) — hits a wave of users on older ComfyUI builds that can't load the new text-encoder weights. | Fix per affected users (not OP/maintainer): fully update the ComfyUI backend+frontend — diverHansun confirmed updating to latest solved it, seanntxj fixed it by force-updating via command line instead of ComfyUI-Manager (one user still broken on 0.3.76 after Manager update). diverHansun links the official comfyui-wiki update guide. | [#339](https://github.com/Comfy-Org/workflow_templates/issues/339) |
| 34 | ComfyUI | MEDIUM | LTX 2.5 lip sync: the LTX 2.3 pattern (audio encode -> latent mask -> audio latent) no longer produces synced mouth movement; modality/CFG/mask tweaks don't help. | UCHIJ (C3) ported the 2.3 lipsync nodes to a native-node LTX 2.5 workflow that works: https://github.com/UCHIJ/LTX2.5-LipSync. Not confirmed by OP. | [#15643](https://github.com/Comfy-Org/ComfyUI/issues/15643) |
| 35 | ComfyUI-KJNodes | MEDIUM | Feature request revealed a real bug: pasted JSON updates the on-canvas regions (after maintainer fixed the stale json input slot) but the node's final prompt output still uses the original content. | Maintainer kijai (2026-06-04) said the json input slot bug 'should be fixed now' and copy/paste buttons update the node; OP confirmed canvas editing works but output still stale — unresolved. | [#651](https://github.com/kijai/ComfyUI-KJNodes/issues/651) |
| 36 | ComfyUI-GGUF | MEDIUM | Krea-2 GGUF (realrebelai diffusion_models + unsloth Qwen3-VL text encoder) not supported by mainline nodes; Krea-2 layering also emits benign 'unet unexpected: last.down/up.weight' warnings. | Community forks work: m8rr (Dynamic-VRAM branch) reports 'krea2 UNet and TE both function properly using GGUF'; RealRebelAI has a forked loader but no PR was merged and no OP confirmation (m8rr comment 2026-06-24). | [#464](https://github.com/city96/ComfyUI-GGUF/issues/464) |
| 37 | ComfyUI-GGUF | MEDIUM | Ideogram-4 with unsloth Qwen3-VL GGUF text encoder unsupported; qwen3vl TE GGUFs load as wrong model type (Flux2TEModel_) and vision/mmproj path needs explicit support. | m8rr commits (a963418, 249624c, c7fb703 on his fork) add qwen3vl TE + vision support via mmproj file; fxd0h notes the official qwen3vl_8b_fp8_scaled safetensors through standard CLIPLoader is enough to run Ideogram-4 end to end, making a GGUF TE optional. No maintainer/OP confirmation on mainline. | [#452](https://github.com/city96/ComfyUI-GGUF/issues/452) |
| 38 | ComfyUI-GGUF | MEDIUM | Gemma-4 GGUF as text encoder for TextGenerate node unsupported by mainline nodes (body empty; request carried by title). | m8rr's Dynamic-VRAM fork runs gemma-4-E2B GGUF but 'only under E2B+BF16(?), highly restrictive, audio input not supported'; molbal's fork added Gemma4 support (commit 1d43c20). No maintainer action or OP follow-up. | [#450](https://github.com/city96/ComfyUI-GGUF/issues/450) |
| 39 | workflow_templates | MEDIUM | video_wan2_2_14B_animate template opens with 2 missing nodes ('Cannot execute because a node is missing the class_type property'); ComfyUI-Manager shows nothing to install and restarts don't help. A second user (Watermelon6093) reports the same. | Maintainer (comfyui-wiki): 'This model support requires custom nodes. Please follow the note in the guide to install the custom nodes.' Users report the Manager finds no such nodes (same family as #422), so no confirmed install path. | [#447](https://github.com/Comfy-Org/workflow_templates/issues/447) |
| 40 | ComfyUI-LTXVideo | MEDIUM | LTX 2.3 image-audio-to-video: lips never move / no lip-sync to the provided audio for most users, video and audio otherwise generate fine. | Several community workarounds, none official: remove/bypass the Prompt Enhancer group and explicitly write that the character 'lip-syncs their dialog' in the prompt (rocko107: 'I did get it to work'); raise SolidMask values 0.2→0.5 (ZELBIO); nag scale 3.2–4.2 plus negative prompt (othello77); other users report these don't work for them. | [#438](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/438) |

## C. Generalizable — no solution found

| # | repo | difficulty | problem | issue |
|---|------|-----------|---------|------- |
| 41 | ComfyUI | HARD | Native TRELLIS 2 / Pixral3D workflow: Sparse Structure Generation ok, but KSampler in Shape Generation fails with ValueError 'not enough values to unpack (expected 4, got 3)' on torch 2.12.1+cu130 (int8 and fp16 both). | [#16028](https://github.com/Comfy-Org/ComfyUI/issues/16028) |
| 42 | workflow_templates | HARD | LTX 2.3 image-to-video template (video_ltx2_3_ia2v.json) outputs corrupted latent-noise/glitch video from the first frame on RTX 5090 with latest ComfyUI; persists across prompts, VAE settings, resolutions, and custom node sets, with no log errors. | [#871](https://github.com/Comfy-Org/workflow_templates/issues/871) |
| 43 | ComfyUI-LTXVideo | HARD | Official workflow fails with ltx-2.3-22b-dev-int8-ConvRot.safetensors: LTXVAudioVAELoader cannot load the audio component of the int8-ConvRot quantized model — loader/format support gap. | [#529](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/529) |
| 44 | ComfyUI | MEDIUM | MiniMax H3 base works but adding the latent upscaler (dasiwa workflow) crashes SamplerCustomAdvanced with shape mismatch [1032,96] vs [4128,96]. | [#15968](https://github.com/Comfy-Org/ComfyUI/issues/15968) |
| 45 | ComfyUI | MEDIUM | Official MiniMax H3 embeddings (e.g. minimaxh3_bullet_time) appear to work with a reference image but are ignored in pure text2img with identical prompt/seed. | [#15862](https://github.com/Comfy-Org/ComfyUI/issues/15862) |
| 46 | ComfyUI | MEDIUM | LTX 2.5 22b dev Q5_1 GGUF in the default template produces very grainy output; user tried basic/LTXV schedulers and 25/4 step configs without success. | [#15693](https://github.com/Comfy-Org/ComfyUI/issues/15693) |
| 47 | ComfyUI | MEDIUM | T5 load fails with size mismatch for shared.weight (256384 vs 32128 vocab) - the placed text-encoder file doesn't match the workflow/model; only reply asks which workflow. | [#15554](https://github.com/Comfy-Org/ComfyUI/issues/15554) |
| 48 | ComfyUI | MEDIUM | Local standard H3 Ref2v workflow produces no reference audio (occasional click) and random lip motion while the same inputs via the H3 API work perfectly, with and without turbo LoRA. | [#15470](https://github.com/Comfy-Org/ComfyUI/issues/15470) |
| 49 | ComfyUI-WanVideoWrapper | MEDIUM | FantasyTalkingModelLoader fails with KeyError 'proj_model.proj.weight' loading fantasytalking_fp16.safetensors in the standard WanVideo I2V + FantasyTalking workflow — checkpoint keys don't match what the node expects (file/version mismatch). 0 comments, dead. | [#2001](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/2001) |
| 50 | ComfyUI-WanVideoWrapper | MEDIUM | WanAnimate test workflow (context options enabled, KJ embedding+sampler combo) crashes during sampling with 'index 2 is out of bounds for dimension 0 with size 2' — same IndexError signature as #1997 (bg_images length desync with context windowing). | [#1971](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/1971) |
| 51 | ComfyUI-WanVideoWrapper | MEDIUM | Running the SCAIL workflow with the distilled LoRA bypassed (CFG 4, 28 steps) consistently produces severe purple tint/color burn; asks for base-model settings. Reproducible for anyone using base SCAIL without the distill LoRA; 0 comments, dead. | [#1963](https://github.com/kijai/ComfyUI-WanVideoWrapper/issues/1963) |
| 52 | workflow_templates | MEDIUM | Flux.1 Dev OneReward inpainting template returns unchanged or wrong images when the prompt asks to add new objects (pillows, plants) with or without a mask. Likely a model-capability limitation of OneReward inpainting rather than a wiring error, but undiagnosed. | [#913](https://github.com/Comfy-Org/workflow_templates/issues/913) |
| 53 | ComfyUI-KJNodes | MEDIUM | Image Batch Extend (overlap) produces a colour shift between the source and extended segments in video-extension workflows, reproducible regardless of stitching method. | [#617](https://github.com/kijai/ComfyUI-KJNodes/issues/617) |
| 54 | ComfyUI-KJNodes | MEDIUM | Loading LTX23_video_vae_bf16.safetensors (LTX 2.3 VAE) into a VideoVAE built for the older LTX 2.0 VAE architecture fails with decoder.conv_in shape mismatches (1024 vs 256 channels) — model/workflow version incompatibility. | [#591](https://github.com/kijai/ComfyUI-KJNodes/issues/591) |
| 55 | ComfyUI-KJNodes | MEDIUM | AttributeError: module ...comfyui-videohelpersuite has no attribute 'load_video_nodes' — a workflow/node referencing VHS's removed/renamed internal API breaks with latest VHS. | [#574](https://github.com/kijai/ComfyUI-KJNodes/issues/574) |
| 56 | ComfyUI-LTXVideo | MEDIUM | IA2V template lip-syncs well but invents new characters, moves the camera unrequested, and ignores prompt instructions — prompt/adherence problem in the official template affecting the reporter. | [#480](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/480) |
| 57 | ComfyUI-GGUF | MEDIUM | Converting the Sulphur-2 (LTX 2.3) model to GGUF yields 'unrecognized model' — the quantization tooling doesn't recognize the architecture. No answers in thread. | [#451](https://github.com/city96/ComfyUI-GGUF/issues/451) |
| 58 | ComfyUI-LTXVideo | MEDIUM | LTX 2.3 Image-to-Video template forces its pre-loaded reference image (a child's drawing of a woman) into every generation even with text-to-video enabled — output wildly off-prompt for multiple users. | [#447](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/447) |
| 59 | ComfyUI-GGUF | MEDIUM | HiDream-O1-Image GGUF not loadable; request for arch support (noted as important for low-VRAM GPUs). No solution offered. | [#446](https://github.com/city96/ComfyUI-GGUF/issues/446) |
| 60 | ComfyUI-LTXVideo | MEDIUM | LTX 2.3 IA2V video duration cannot be changed from 9 — other lengths break lip movement (human and animal lips). | [#441](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/441) |
| 61 | ComfyUI-GGUF | MEDIUM | babakarto z_image_base_Q8_0.gguf fails for multiple users in load_diffusion_model_state_dict → convert_diffusers_mmdit: 'expanded size of the tensor (3840) must match the existing size (4080)' — reproducible for anyone using this model file. | [#431](https://github.com/city96/ComfyUI-GGUF/issues/431) |
| 62 | workflow_templates | MEDIUM | Flux 2 template fails validation before execution: EmptyFlux2LatentImage width/height and Flux2Scheduler width/steps arrive as None ('Failed to convert an input value to a INT value'), suggesting unprompted/unpopulated widget values in the shipped template. | [#330](https://github.com/Comfy-Org/workflow_templates/issues/330) |

## D. Core bugs with confirmed fixes

| # | repo | problem | solution | issue |
|---|------|---------|----------|------- |
| 63 | ComfyUI | RX 9060 XT crashes with 'CUDA error: invalid argument' running the official MiniMax H3 t2v workflow (AOTriton/SDPA kernel path). | Fixed by PR #15648. 0xDELUXA (C0/C1) diagnosed the missing-AOTriton-kernel root cause and suggested --use-split-cross-attention as a test; OP (C2) confirms 'It is solved after merging #15648' and C5 confirms the merge. | [#15653](https://github.com/Comfy-Org/ComfyUI/issues/15653) |
| 64 | ComfyUI | MiniMaxH3MemoryEfficientSageAttentionPatch fails with 'sageattention is not new enough version or could not determine CUDA architecture' despite correct torch 2.13+cu130 / sageattention 2.2.0 - node's own version/arch check bug. | Workaround confirmed by OP (C5) and others: swap the node for 'Patch Sage Attention KJ' (aakash-chaddha, C2) - after which the native H3 patch also started working again for some. Real fix tracked in PR #15270 (still open at thread end). | [#15478](https://github.com/Comfy-Org/ComfyUI/issues/15478) |
| 65 | ComfyUI | On ROCm, model loading became ~15x slower: per-tensor ~0.5s fixed overhead when .to(device) copies mmap-backed safetensors tensors (cost scales with tensor count). | OP guayar (C1) root-caused it and --disable-mmap 'completely fixed' load times; independently confirmed by zenmasterzardoz (C2, gfx1100) and boxwrench (C4, 0.33.2). Underlying per-tensor mmap-transfer defect still needs a core fix honoring disable_mmap everywhere. | [#15001](https://github.com/Comfy-Org/ComfyUI/issues/15001) |
| 66 | ComfyUI-Manager | CNR-only (registry-published) nodes never get the 'update-state' field set in the Manager node list (only 'updatable' is), so the frontend never shows the update button for registry nodes even when a newer version exists. | OP confirmed a one-line fix in manager_core.py (~line 3215, where the CNR node dict is built): add item['update-state'] = 'true' if updatable else 'false' next to the existing updatable field — update buttons now appear. | [#3220](https://github.com/Comfy-Org/ComfyUI-Manager/issues/3220) |
| 67 | ComfyUI-Manager | With --enable-manager-legacy-ui, 'update all'/'update comfyui' crashes with AttributeError: 'NoneType' object has no attribute 'content_type' in manager_security.reject_simple_form_post, introduced by PR #2819 (CSRF hardening). | Commenter Phosay identifies PR #2819 as the cause; hansnolte confirms the workaround: uncheck --enable-manager and pass only --enable-manager-legacy-ui (not both flags), verified working by GroxicTinch. | [#2843](https://github.com/Comfy-Org/ComfyUI-Manager/issues/2843) |
| 68 | ComfyUI-LTXVideo | LTX IC Lora Loader (Model Only) shows as unknown/missing because the whole ComfyUI-LTXVideo pack fails to import: kornia 0.8.x no longer re-exports `pad` from kornia.geometry.transform.pyramid, killing pyramid_blending.py at import time. | Community fix (posted by OP Magenta-6 from Gemini diagnosis, referencing #494): remove `pad` from the kornia import block in pyramid_blending.py and add `from torch.nn.functional import pad`. OP confirmed 'THIS FIXED THE PROBLEM', and two later commenters confirm; caveat: re-appears after node updates until PR merges. | [#553](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/553) |
| 69 | ComfyUI-LTXVideo | kornia 0.8.3 removed the accidental re-export of torch's `pad` from kornia.geometry.transform.pyramid, so the whole node pack fails to import and every dependent workflow breaks. | kornia maintainer (ducha-aiki) confirmed cause and that the fix belongs in this repo (PRs #498/#506/#516): drop `pad` from the kornia import and use `pad = F.pad` (confirmed by MartinT23); alternative confirmed workaround: pin kornia==0.8.2 (dadleo). | [#542](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/542) |
| 70 | ComfyUI-LTXVideo | Fix proposal for the kornia>=0.8.x ImportError: `from kornia.geometry.transform.pyramid import pad` no longer resolves, preventing the entire package from loading. | OP's diff (remove `pad` from kornia import, add `pad = F.pad`) confirmed working by charlyarias65 in comments. | [#541](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/541) |
| 71 | ComfyUI-LTXVideo | After a ComfyUI update, the pack fails to import: `cannot import name 'interleaved_freqs_cis' from 'comfy.ldm.lightricks.model'` — ComfyUI refactored RoPE helpers (merged into freqs_cis_matrix) and older node-pack code no longer imports. | Fixed by updating ComfyUI-LTXVideo via git pull to get the compatibility fix (PR #532) — GlamoramaAttack confirms 'everything should work again'; interim: clean manual reinstall (Dunc4n1dah0) or hand-edit embeddings_connector.py to use freqs_cis_matrix (screwballl). | [#538](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/538) |
| 72 | ComfyUI-LTXVideo | On ComfyUI 0.28.0 the node pack fails to load with `cannot import name 'interleaved_freqs_cis'` — core RoPE refactor removed the function the pack imports. | GlamoramaAttack (twice in related threads): `git pull` the custom node and it works again — the repo landed the 0.28 compatibility fix (PR #532); the suggested kornia==0.6.12 downgrade in-thread is unrelated to this error. | [#537](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/537) |
| 73 | ComfyUI-LTXVideo | After updating ComfyUI to 0.28, import fails with `cannot import name 'interleaved_freqs_cis'` — same core RoPE refactor break as #531/#537. | GlamoramaAttack: either pin ComfyUI '0.28.2' (actually a newer build where it's fixed) or update the custom node; final confirmation 'Update with git pull for this custom-node and everything is fine again' (PR #532). | [#535](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/535) |
| 74 | ComfyUI-LTXVideo | On kornia >= 0.8.3 the pack fails to import (`cannot import name 'pad'`) because kornia no longer re-exports torch's pad from the pyramid module. | OP's one-line shim (`pad = F.pad`, drop pad from kornia import) and pinning kornia==0.8.2 (maoper11) both confirmed working by later commenters in #494/#517. | [#525](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/525) |
| 75 | ComfyUI-LTXVideo | Installation/import fails: `cannot import name 'pad' from 'kornia.geometry.transform.pyramid'` (kornia 0.8.3 installed, pack expects the old re-export), leaving IC-LoRA nodes missing. | kklang774's fix (`pip uninstall -y kornia kornia-rs; pip install kornia==0.7.3`) confirmed by altanwalk; equivalent one-line edit of pyramid_blending.py to import pad from torch.nn.functional confirmed by KarinMiku and HaidukiHaruka; PR #506 remained unmerged. | [#517](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/517) |
| 76 | ComfyUI-LTXVideo | Import error: `cannot import name 'pad' from 'kornia.geometry.transform.pyramid'` with kornia 0.8.3 — same dependency break as #494. | Confirmed workarounds: downgrade kornia (0.7.3 works per yyyingjjjie, 0.6.12 per alephba) or apply the one-line pad import fix (PR #506 by DevNullInc, unmerged at the time). | [#505](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/505) |
| 77 | ComfyUI-LTXVideo | Pack does not import with newer kornia (0.8.3 dropped the `pad` re-export); OP confirms downgrading kornia fixes the manager 'import failed' error. | Downgrade: `pip install kornia==0.8.2` (VladiCz confirms working on ComfyUI 0.28.0); note drowai: 0.6.12 downgrade breaks newer node packs on fresh installs — proper fix is the pyramid_blending.py patch (PR #506). | [#496](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/496) |
| 78 | ComfyUI-LTXVideo | Canonical report of `cannot import name 'pad' from 'kornia.geometry.transform.pyramid'` after kornia 0.8.3 removed the accidental torch.nn.functional.pad re-export — pack-wide import failure. | Multiple confirmed fixes: pin kornia 0.8.2/0.7.3/0.6.12, or one-line patch importing pad from torch.nn.functional in pyramid_blending.py (omniscia's patch confirmed by nammmmm); PRs #506/#516/#498 sat unmerged while the break persisted for months. | [#494](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/494) |
| 79 | comfy-aimdo | With sageattention2 installed alongside comfy-aimdo (RTX 5090, CUDA 13.2, torch 2.12.0+cu132, ComfyUI 0.25.0), ComfyUI crashes on startup; removing sageattention2 or passing --disable-dynamic-vram both avoid it. Conflict between sageattention2's and aimdo's CUDA API hooking; coderabbit flagged issue #75 (CUDA 13.0 Detours hook failure) as a possible duplicate. | Workaround confirmed by OP in the issue body: launch with --disable-dynamic-vram (or uninstall sageattention2) and startup works 'perfectly well'. Root-cause fix (hook installation on CUDA 13.x) tracked via related issue #75; no maintainer fix confirmed in this thread (no comments). | [#64](https://github.com/Comfy-Org/comfy-aimdo/issues/64) |
| 80 | ComfyUI-GGUF | Two code defects block Ideogram-4 GGUF: (1) sd.cpp-style files lack general.architecture and detect_arch has no Ideogram-4 entry → 'Unknown model architecture'; (2) on MPS, quantized GGMLTensor backed by packed uint8 breaks models that do x.to(weight.dtype) before linear ('MPS device does not support linear for non-float inputs'). | OP developed and on-device-verified fixes filed as PRs #455 and #456 (arch detection + MPS dtype handling), also available in fork github.com/fxd0h/ComfyUI-GGUF; report states Ideogram-4 GGUF 'loads and generates on Apple Silicon... verified end to end'. | [#454](https://github.com/city96/ComfyUI-GGUF/issues/454) |
| 81 | comfy-aimdo | aimdo's vbar_allocate (src/model-vbar.c:222-227) clamps each VBAR VA reservation to vram_capacity, so on large-VRAM ROCm/Windows devices every model >=1/10 of device capacity reserves the full capacity of address space; three models (LTX video+TE+VAE) exhaust the ~256 GiB per-process GPU VA space and sampling fails with hipErrorOutOfMemory while 60-82 GB VRAM is free. | OP demonstrated that changing one multiplier in ComfyUI (the ModelVBAR(self.model_size() * 10, ...) reservation in model_patcher.py _vbar_get) takes peak usage on a 43 GiB LTX workflow from 26,978 MB to 81,522 MB and turns the hard failure into a clean run (OP-confirmed measurement in issue body). No upstream aimdo fix confirmed yet; merged PR #78 (NVML pressure and async pre-free clamping) was bot-linked as related but not confirmed to fix this. | [#83](https://github.com/Comfy-Org/comfy-aimdo/issues/83) |
| 82 | comfy-aimdo | On CUDA 13.0 (nvcuda64.dll returning real function bodies with 14-byte prologues), Microsoft Detours fails to install aimdo's 6 CUDA hooks (DetourAttach error 8, DetourAllocateRegionWithinJumpBounds returns NULL), causing an access-violation crash at ComfyUI startup in control.py init_devices. | OP (community contributor 'openwork') provides a full fix in the issue body: abandon Detours and implement manual x64 hooks (absolute indirect JMP 'FF 25 00 00 00 00 + 8-byte addr' trampoline), plus fix RIP-relative displacement overflow via near allocation; validated 7+ days of daily use without crashes. Author caveats the fix may only apply to their own case; not confirmed merged upstream. | [#75](https://github.com/Comfy-Org/comfy-aimdo/issues/75) |
| 83 | comfy-aimdo | aimdo's Windows safetensors handling opens files with FILE_FLAG_SEQUENTIAL_SCAN, which makes Windows discard the cached data after first read; users with large RAM (512 GB) see models re-read from physical disk on every generation, and Bit-Nibble's PERFMON A/B test (aimdo build with vs without the flag) confirmed the flag causes repeated physical disk reads instead of OS-cache hits. | Fix merged via PR #76 (linked by maintainer rattus128 in comment: 'https://github.com/Comfy-Org/comfy-aimdo/pull/76'), flowed into comfy-aimdo then ComfyUI; brendanhoar confirms (2026-08-30 comment) 'This appears to be resolved from my perspective... the problem went away that day'. Additional OP-confirmed Linux workaround from VladiCz: adding --disable-pinned-memory (with --high-ram) restored ~10x faster model loads after ComfyUI 0.22.0. | [#70](https://github.com/Comfy-Org/comfy-aimdo/issues/70) |
| 84 | ComfyUI | Official MiniMax H3 workflow doubles in runtime (12 -> 25+ min) after the 0.31 update on 5080/5090/H200/5060Ti; VRAM underused, disk thrashing each gen. | OP (C2) and partridge (C5): rolling back to 0.30.0 restores 12-min runtimes. comfyanonymous (C9) suggested renaming custom_nodes to rule them out - partially helped one user then reverted. Root regression in 0.31 memory management left unfixed. | [#15445](https://github.com/Comfy-Org/ComfyUI/issues/15445) |
| 85 | ComfyUI | After the pinned-memory update, WAN 2.2 (and later MiniMax H3) intermittently fails at 'HostBuffer.read_file_slice failed'/CUDA OOM during weight loading, mostly on multi-GPU systems. | OP-confirmed fixes: --disable-pinned-memory (title/body), and eoris' CUDA_VISIBLE_DEVICES=1 / --cuda-device <uuid> to hide the second GPU (C6, and akashzeno C9's controlled A/B). rattus128 worked a speculative fix in PR #14252; aimdo 0.4.5+ 'cold marking of pins' also shipped. | [#14250](https://github.com/Comfy-Org/ComfyUI/issues/14250) |
| 86 | ComfyUI | Custom LoRA-trainer (block swapping) OOMs at pinned-memory allocation on >=0.16 in WSL/5090 although RAM/VRAM are free; worked 21h on 0.9.x. | Booyaka101 root-caused it: comfy-aimdo is still dlopen'd (RTLD_GLOBAL) on WSL by main.py line 45 even with dynamic VRAM disabled. OP (C1/C12) confirms --disable-dynamic-vram fixes it and that commenting out the dlopen confirms the diagnosis; fix proposed in PR #13818 (WSL guard), not yet merged at thread end. | [#13458](https://github.com/Comfy-Org/ComfyUI/issues/13458) |
| 87 | ComfyUI-KJNodes | MiniMax H3 Low/VRAM attention node fails with minimax_block_lowmem_forward() got an unexpected keyword argument 'attention' after upstream model API changes. | Maintainer kijai (2026-09-06): 'Should be fixed now' via recent PR; also notes the low-VRAM attention is no longer needed with comfy core sparse attention. OP confirmed (2026-09-08) the H3 Low VRAM attention breakage is fixed. Side-thread: VRAM/OOM with Model Preview Override traced to comfy-compiler/aimdo, mitigated by --disable-comfy-compiler. | [#750](https://github.com/kijai/ComfyUI-KJNodes/issues/750) |
| 88 | ComfyUI-LTXVideo | Several LTX nodes error after install — pack import failure after a ComfyUI update (same class as #538 interleaved_freqs_cis break), so nodes fail to register. | Danny-707 links the #538 fix (remove/reinstall or git pull the node pack) and peterducan-hub confirms it worked; OP separately confirmed a clean 'ComfyUI easy install' fixed it. | [#544](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/544) |
| 89 | ComfyUI-LTXVideo | Pack import fails after ComfyUI update (commit 7c59a078): `cannot import name 'interleaved_freqs_cis' from 'comfy.ldm.lightricks.model'`. | rmarma posted a patch switching embeddings_connector.py to `freqs_cis_matrix`; multiple users confirmed hand-applying the commit changes works (DracoLockhard, fafayqa with replacement file); ultimately fixed by git pull once PR #532 landed. | [#531](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/531) |
| 90 | ComfyUI-LTXVideo | STGGuiderAdvanced with apply_apg=True crashes: stg.py line 460 passes momentum_buffer=None to local apg() which doesn't accept it (signature drift vs upstream APG). | OP root-caused it and confirmed the workaround: remove `momentum_buffer=None,` from the apg() call at stg.py line 460 — 'resolves the issue and the sampling is working again'. | [#492](https://github.com/Lightricks/ComfyUI-LTXVideo/issues/492) |
| 91 | ComfyUI-GGUF | LTX-2.5 GGUF models have 3 BF16 non-layer params (learnable_registers, keyframes_abs_pos_embedding) left as GGMLTensor, so packed BF16 storage dims (e.g. 4096 vs 8192) leak into runtime ops and crash samplers. | OP-verified fix: after gguf_sd_loader(), materialize only those three named BF16 tensors via dequantize_tensor() before load_diffusion_model_state_dict(); full workflow then runs (OP offers a PR for this in loader.py). | [#477](https://github.com/city96/ComfyUI-GGUF/issues/477) |
| 92 | ComfyUI-GGUF | ComfyUI-GGUF 2.0.0 on Windows: GGUFModelPatcher.unpatch_model with unpatch_weights=True touches GGUF mmap'd raw tensor memory during cache cleanup → access-violation crash (regression from 2.0.0 refactor; 1.1.6 had the fix). | OP gives definitive cause + one-line fix: pass unpatch_weights=False to super().unpatch_model(device_to, unpatch_weights=False) so the base patcher never touches mmap weight memory; model still unloads normally, no crash (OP-verified). | [#444](https://github.com/city96/ComfyUI-GGUF/issues/444) |
| 93 | SageAttention | MiniMax H3 generation in ComfyUI via KJNodes' MiniMaxH3MemoryEfficientSageAttentionPatch hard-crashes the CUDA context on SM89 (L20) inside the int8-QK/FP8-PV kernel path (_sageattn_int8_fp8_nhd); KJNodes PR #748 V-offset fix does not help. | OP A/B confirmed a settings-level workaround: switch the 'Patch Sage Attention KJ' mode to sageattn_qk_int8_pv_fp16_cuda and the same workflow runs cleanly — crash is specific to the SM89 FP8-PV kernel, which remains unfixed upstream. | [#403](https://github.com/thu-ml/SageAttention/issues/403) |
| 94 | SageAttention | MiniMax H3 generation with the int8-QK/per-channel-FP8-PV Sage kernel (_sageattn_int8_fp8_nhd) loses the GPU at the Windows driver level (nvlddmkm, requires reboot) on RTX 5060 Ti; a single-variable control shows native attention succeeds at higher VRAM/power, isolating the failure to the kernel. | OP-confirmed workaround (comment by leimuxiao, 2 clean 260s runs): keep int8-QK, demote V from per-channel FP8 to plain FP16 and call the sm80 kernel _qattn_sm80.qk_int8_sv_f16_accum_f32_attn instead of the sm89 fp8-PV kernel — no GPU loss, still ~21% faster than native. FP8-PV on this path remains broken upstream. | [#391](https://github.com/thu-ml/SageAttention/issues/391) |
| 95 | SageAttention | sageattn3_blackwell throws 'CUDA error: misaligned address' at inference on RTX 5060 Ti (sm_120) while SageAttention 2.2 works on the same system — defect in the Blackwell kernel implementation, not the user's workflow. | Maintainer woct0rdho pointed to the mengqin/SageAttention fork and PR #323; OP confirmed: clone that fork, add -allow-unsupported-compiler to the Windows setup.py branch, install with pip install --no-build-isolation -e . — misaligned-address error fully resolved; second user (bfreshour) confirmed on a 5070 Ti. Caveat: modest real-world speedup vs int8/fp8 path. | [#357](https://github.com/thu-ml/SageAttention/issues/357) |
| 96 | comfy-kitchen | MiniMax H3 templates crash on AMD (gfx1151) with `sage int8 attention: Q/K base pointers and B/H/N strides must preserve 4-element alignment` even though the tensors pass alignment probes. Contributor root-caused it to torch 2.9.1's DLPack export rewriting strides for size-1 dimensions, not a workflow or AMD-specific mistake. | Contributor 0xDELUXA identified torch 2.9.1 DLPack stride rewriting as the cause; two confirmed routes: (1) upgrade ROCm/torch to TheRock nightly (OP confirmed working, ~2x faster), and (2) a comfy-kitchen fix (contiguous fallback in shared sage_attention.py) merged and shipped in commit 7c6ca3a, which OP confirmed works on torch 2.9.1. | [#110](https://github.com/Comfy-Org/comfy-kitchen/issues/110) |
| 97 | comfy-kitchen | Hard crash when rendering with the Triton backend on AMD, MLIR dump blaming dequantize_nvfp4_kernel_tl while running the nvfp4 text encoder (Klein 9b / MiniMax H3 templates). Affects all users running nvfp4 text encoders with the Triton backend. | Fixed by PR #47 (check preventing nvfp4 TE from taking the broken Triton path), merged to master per commenter sleppyrobot who confirms it 'did allow nvfp4 TE for miniH3 to work with enable-triton-backend'. Maintainer 0xDELUXA also notes Triton is now only a fallback on AMD — use the HIP backend. | [#71](https://github.com/Comfy-Org/comfy-kitchen/issues/71) |
| 98 | comfy-kitchen | amdgpu page fault in apply_rope_kernel (pinned via AMD_SERIALIZE_KERNEL=3/AMD_LOG_LEVEL=3) when running Krea 2 (INT8 and even BF16) with the Triton backend on gfx1151; Krea 2's specific tensor shapes/layout hit the CONVROT rope kernel. Reproduced by multiple users; also crashes with fp8. | Fixed by PR #77 (referenced by sleppyrobot as covering the apply_rope fault); OP waltercool confirms 'this apparently works fine on my local using latest ComfyUI'. Interim workarounds confirmed in thread: PR #56 patch for eager INT8 speed and the ComfyUI-INT8-Fast-ROCM custom node (~49s vs 210s eager INT8); maintainer says use HIP backend instead of Triton. | [#64](https://github.com/Comfy-Org/comfy-kitchen/issues/64) |
| 99 | comfy-aimdo | On NVIDIA GRID/vGPU (RTX6000-24Q, A40-24Q), ModelVBAR(self.model_size()*10) raises a hard MemoryError('VBAR allocation failed') during model load because cuMemAddressReserve-based VBAR allocation fails in virtualized environments, instead of degrading gracefully to standard model management. Affects all vGPU/virtualized-GPU users; another user reports PyTorch >2.8.x correlation. | Maintainer rattus128 built a new wheel (actions run 23231132334) with a startup self-test that probes the required CUDA APIs and wholesale disables the DynamicVRAM system when they fail; OP gregdotcom confirms 'Thanks for the new build, working fine now' with full success logs. Maintainer notes --novram mode is similar but switches aimdo off entirely, and suggests a no-vbar DynamicVRAM mode belongs in core ComfyUI. | [#25](https://github.com/Comfy-Org/comfy-aimdo/issues/25) |
