1. **P1 — Runaway exactly-once contract remains sequencing-ambiguous.**  
   §VII.1–3 makes `test_generated_temporary_project_round_trip` the sole Runaway replacement gate and requires its execution exactly once in the authoritative full suite. However, WP3 (§IX) separately requires validation of the “generated Runaway temporary-project migration and reopen” before Boundary B, and WP6’s “remaining source scenario checks” also includes migration/reopen scenarios. The plan does not define whether these are migration-only checks or executions of the canonical fixture. This permits duplicate expensive round-trip execution or an unreceipted pre-full-suite execution, violating §VII and the anti-pattern “No duplicate Runaway round-trip invocation.” The WP3 check must be explicitly disjoint from the canonical fixture, or removed.

2. **P1 — Criterion 15 has a final-review/evidence-matrix ordering cycle.**  
   §XI requires every row—including row 15—to contain an existing, digest-bound reviewer artifact with disposition `pass`; row 15 specifically covers the “independent oracle disposition.” WP6 generates and checks the final matrix before §XIII says to obtain the final Sol oracle review. If the reviewer artifact is the final oracle receipt, it cannot be referenced when the matrix is first checked. If the oracle reviews that matrix and the matrix is regenerated afterward to include the receipt, the oracle reviewed a different matrix digest, making the review stale. No receipt-binding or two-stage matrix protocol breaks this cycle. Criterion 15 therefore lacks a realizable criterion-to-reviewed-artifact path.

**North Star disposition:** not clean. Canonical ownership and anti-pattern controls are otherwise aligned, but these two closure-order defects leave duplicate-proof and final evidence authority unresolved.
0