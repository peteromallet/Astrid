# Agent goal — portable unified execution planning handoff

## Objective

Prepare a ready-to-execute Megado plan for the v3 end state: all supported
Wan2GP, VibeComfy-pip, and VibeComfy-checkout work enters the Runtime-owned
task/lease/CAS/settlement lifecycle and runs through one engine-neutral
`GenericPackHost` / `SessionManager` path, with typed Astrid adapters,
evidence-backed lifecycle and warmth semantics, legacy authority removed, and
the 45-row historical obligation set closed 45/45 or as an honest terminal.

## Authority and source

The sole current authority is `authority/UNIFIED-PLAN-v3.md`, SHA-256
`0772f5ac5196b7c0e28d19c1d636f18c26a3cc8c550f9f1a09bd9a4e2ddb7dc0`.
`northstar.md` is the adopted direction. `criteria.md` contains the complete
original C01–C16 acceptance definitions. `authority/ADJUDICATION.md` is only
the last formal ruling, extracted as planning evidence.

## Scope and non-goals for this handover

This artifact packages planning records only. It does not authorize product
edits, source worktrees, tests, GPU/provider calls, deployment, delivery
dispatch, old-ledger edits, merge, or release. The receiving mode is
`planning_only`; a future delivery authorization must be explicit and must
preserve the single authoritative `run.yaml`.

Future implementation scope is P0 custody/provenance/capacity, P1 Vibe
integration, P2 manager core, P3 harness conversion, P4 checkout proof, P5
Wan residency feasibility, P6 embedded retention, and P7 final acceptance as
defined by v3.

## Preserved state and stop conditions

The adjudicated state is 22 accepted scoped closures, one reopened `B03-T01`
HC-04 correction, and 22 original pending rows. No pending row closes by
discovered evidence. Runtime local custody is currently unusable and needs
recovery. Historical provider spend and attempt balance are unresolved; the
exact Fire-19 checkout backend is missing; and provider cleanup is not
independently certified. P0.d must reconcile and sign remaining allowance
before any live/GPU fire. P0.b must use bounded recovery or reconstruction,
never wholesale overlay restoration.

## Completion of this planning handoff

Planning completion means the authority, criteria, objective, task graph,
roles/budgets, authorization, provenance, and read-only historical obligation
record are portable and internally consistent. It makes no product-test,
acceptance, 45/45, frozen-composition, or zero-pods claim. The optimistic v3
estimate is ten working days (roughly two calendar weeks), subject to the
unresolved custody, budget, backend, capacity, and evidence-integrity risks.
