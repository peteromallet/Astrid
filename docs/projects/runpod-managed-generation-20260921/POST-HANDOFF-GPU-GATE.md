# L — deferred post-handoff GPU/RunPod qualification

Status: **DEFERRED_POST_HANDOFF / NOT AUTHORIZED / NOT SCHEDULED**.

L is separate from every current P0–P11/Q/H project and from their executable
build/test scope, acceptance criteria, dependency chain and completion status.
Current projects can finish with CPU/fake evidence under
[execution scope](execution-scope.md). Do not run L during setup, build,
validation, automatic retries, manager wakeups or handoff packaging. Do not
claim a pod, attach to one, query the provider or spend money for this packet.

## Retained strategic requirements

After a separate later user instruction activates L, prepare its bounded test
plan using the final exact source composition and existing scope/review history:

1. Verify the real canonical Astrid task → RunPod/lifecycle → VibeComfy/Wan2GP
   execution path, remote logs and artifact retrieval, managed generation record,
   project/shot association and readable media after remote custody ends.
2. Qualify GPU lifecycle A–D/A–E: native persistence, same-asset reuse,
   changed-asset release, no device overlap, cancellation/restart and V→W→V.
   Observe real hardware/model behavior; fake tests cannot certify it.
3. Qualify immutable runtime/model releases and exact CUDA/driver/node/interpreter
   compatibility on fresh pods, including two-pod persistence/reconstruction,
   backend smoke/capacity, measured cold-start performance and clean shutdown.
4. Exercise real job disconnect/deadline, leave_running and custody/teardown
   behavior with exact owned target identity. Preserve shared storage and outputs.
5. Run the separately selected continuation/creative pilot, decode outputs and
   inspect visual quality. Keep infrastructure and creative acceptance distinct.
6. Use actual caller parity/rollback evidence before separately authorized
   operational migration, production profile promotion or worker retirement.

These retain the former P0 live C-G4 clause; lifecycle native T7/T8 and A–E
requirements; canonical job live checks; golden T5/Q4 and A6–A9 live portions;
dependency D14/live handoff; shot pilots; and Q's former live/native F3 proof.
Earlier documents are traceability sources, not executable runbooks for L.

## Activation and result contract

Activation requires a later explicit instruction for this gate, exact source,
runtime/model/target/storage identity, current ownership, external credential
references and a finite aggregate cost/time cap. Credentials stay outside the
ZIP and Git. Establish allocation, timeout and cleanup policy for that session;
never infer a surviving pod, budget or termination permission from old receipts.
Existing review/oracle counters must be preserved; moving scope here does not
reset them. Do not automatically add new model review rounds.

Record hardware results with source and evidence hashes, commands, target and
runtime identity, costs, decoded-output checks and explicit pass/fail/untested
claims. A failure in L does not retroactively mean the scoped CPU project was
never completed; file source-backed corrective work for the demonstrated defect.
Only passing live evidence can support GPU-qualified or production-readiness
claims. This document contains no live launch or test command by design.
