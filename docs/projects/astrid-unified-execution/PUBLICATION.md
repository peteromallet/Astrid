# Publication and portability record

Publication target: public `peteromallet/Astrid`, branch
`handover/astrid-unified-execution-20260909`, based on published `main`
`8150c3b70887495f0fae4a55c1ac70085a900550`. Only the named project directory
is selected for handover commits. The existing main working tree and all
unrelated dirty/untracked source were excluded and preserved. This publishes
planning artifacts, not implementation changes or a final product candidate.

## Audit scope and omissions

The new package was inspected for credential/token/private-key patterns,
absolute machine paths, private IPs, provider identifiers, and unnecessary
personal data. No such exposure was detected in the selected files. Public
project names, source revisions, acceptance constraints, and budget limits are
intentional planning context. The final adjudication is a selected extract;
machine-local links and the historical provider resource ID were removed and
the transformation is disclosed in that file.

A bounded independent audit of 1,384 reachable host commits found no
private-key or Slack-token signatures; secret-like examples were synthetic
redaction/normalization test fixtures. Dedicated secret-scanner binaries were
not available. This is not a guarantee that all historical secrets or private
details are absent. The host history is already public at the selected origin
base; no snapshot history, archive, receipt collection, or unrelated branch
history is imported, and no history is rewritten.

Omitted: the multi-megabyte adjudication transcript, other exploratory
transcripts, raw Fire-19 inventories, tarballs, Git bundles, machine caches,
the original absolute-path ledger/receipts, and unrelated local work. Their
source hashes and relevant adjudicated conclusions are retained. Missing
execution evidence remains a P0 recovery prerequisite, not acceptance credit.

## Recipient checks

The skill repository was cloned into a fresh directory and checked out at
`1219b3183dde29e4b74681690c4a2351bed3cab3`; the direct-read Megado source
matched that checkout byte-for-byte. The preparing host exposes native
delegation and all configured model names. Receiving-host availability still
requires a local check; model substitution is not implicit.

The portable run configuration parses as planning-only with seven role slots
and no scheduled review stages. The task graph has 21 unique tasks, valid
dependencies and no cycles. The authority digest is unchanged; local document
links and selected criterion/obligation records are checked from the packaged
tree. A clean clone/ref checkout is used before final handoff. These are
packaging checks only; no product test, live GPU journey, or acceptance review
has been run for this handover.

There is no implementation candidate or executable review packet yet. Future
packets must be built from the real frozen candidate and receipts, using the
pinned skill's template; this package supplies the goal, criteria, plan,
role/configuration and historical state inputs only.
