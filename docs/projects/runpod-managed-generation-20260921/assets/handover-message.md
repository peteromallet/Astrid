# Copy-paste receiving-agent mandate

Take over the coordinated `runpod-managed-generation-20260921` program using
this packet's START-HERE.md, PENDING.md, program-manager.md and program-map.md.
Current handoff mode is continuation/control review. The current authorization
is packaging only: no product source edits, active-run changes, billable
resources/pods, pushes or merges. Historical delivery authority in an exported
run is not expanded by this message.

Project A recorded deterministic approval; exact final-SHA binding remains
pending. The user's current scope is CPU/fake/offline build and validation for
all P0–P11/Q/H projects. Read execution-scope.md before any exported control.
Remove live-test clauses from current dispatch/acceptance, preserve their IDs as
DEFERRED_POST_HANDOFF in POST-HANDOFF-GPU-GATE.md, and finish the projects without
waiting for live evidence. Gate L is not authorized or automatically scheduled. Selected source commits are Astrid
`032f65bcd84bd6360f83c260aa902c3a70cc0575`, VibeComfy
`fba5567d4efb59e2ae5020b8ad0d3db11cf02910`, and runpod-lifecycle
`f6a94f52a7f9a5a83e83de46abbec8a9f86dfd77`. The inspected skills baseline is
`f1296034386486d65cf19876d5e5ebfb0547e7cf`; intentional dirty skill-control
exports have separate exact hashes in control-provenance.json. Do not pretend
the baseline contains that overlay or the packet-host base contains new docs.

Objective: deliver the neutral managed result path, lifecycle/bootstrap/device
authority and VibeComfy/Wan2GP adapters, canonical jobs and reproducible
distribution, caller migration readiness with offline rollback proof, and Runtime-owned
shot persistence/input/selection/orchestration. Manage P0–P11 plus Q/H as one
program with F0–F3 checkpoints and exclusive write ownership. Reigh-worker is
transitional bootstrap/readiness and Wan2GP/prebuild substrate; it is not a
managed-result requirement and remains pending migration/retirement.

Start with `python3 tools/packet.py verify .`, then
`python3 tools/receive.py --destination ../NEW-sources --report ../NEW-report.json`.
Use new destinations only. Read setup.md for archive/path checks, public URL
fetch fallback and direct-read skills. The orchestrator pin is unavailable;
reigh-app retrieval is unchecked. Preserve dirty-state gaps and continue only
independent work. Do not use latest refs as silent substitutes.

Use each owning run's exported run.yaml for models, stages and caps; status.md
preserves used counters. A has generalized_handoff_sandbox (G0–G2, max two)
and final_integrated (G0–G4, max three), oracle max three. Exact consumption
must be recovered where absent. Planning-only runs do not gain delivery stages
by being included. Preserve exhausted residency/golden/engine budgets and the
shot critique count. No reviewer/oracle ran for this packaging pass.

Before implementation, establish applicable delivery/source authority, agent
model capability, CPU dependency locks and existing review budgets. Provider
credentials, GPU capacity and live resource budgets are not prerequisites.
Build/validate offline with fake backends, no provider secrets and no GPU access. A wakeup
loop does not survive host loss; unattended operation needs a verified durable
scheduler with single-manager fencing and persisted resume state. Do not claim
live acceptance, model residency, published packet or worker retirement without
source-bound evidence. Finish by returning exact outcome/evidence, unresolved
gates and a refreshed deterministic packet; no PR, merge, deploy or push is
authorized by this delivery message.
