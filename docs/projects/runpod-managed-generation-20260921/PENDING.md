# PENDING — continuation packet, not final program acceptance

Ready for immediate document handoff and independent work planning. Final
CPU/fake/offline project acceptance requires the following refreshes; none is
claimed satisfied. Live qualification is tracked separately in
[deferred gate L](POST-HANDOFF-GPU-GATE.md) and is not a current-project blocker:

1. Bind Project A's recorded deterministic approval to the exact final
   Astrid/VibeComfy/lifecycle source tuple and evidence hashes. The exported
   final review does not freeze its final source SHA. Current C-G4 acceptance is
   deterministic CPU/fake only; its former live clause moved to gate L.
2. Resolve `reigh-worker-orchestrator` pin
   `5f58f30d6ac35a9fce81497680ecdb9ef30b9386`: public exact fetch and bounded
   branch/tag fallback could not retrieve it. Its inspected local checkout has
   unresolved edits/conflicts, excluded from this packet. No latest-ref
   substitution, push or source repair is authorized by packaging.
3. `reigh-app` was added to the repository lock for caller/UI inventory after
   the completed retrieval check. Its locked SHA is an inspected dirty baseline;
   public retrieval is **not checked** in this packet. Source inspection and
   new retrieval were stopped at the user's final instruction. The receiver
   validator covers this entry on its next run.
4. Resolve source dependency-pin differences and produce reproducible CPU build
   recipes, source/wheel locks and model/runtime manifest schemas using fixtures.
   Hardware-qualified locks, model staging and GPU clean-host reproduction move
   to gate L. No model weights or GPU environments are required here.
5. Complete deterministic lifecycle A–D, retention/release, Wan2GP protocol and
   cross-engine handoff fixtures. Actual native GPU persistence, device release
   and RunPod execution are deferred to L; missing GPU access cannot block these
   current projects. Historical closure does not certify hardware behavior.
6. P8–P11 shot persistence/input/selection/orchestration and Q's integrated
   journey require their existing delivery authority and acceptance. The
   consolidated P0–P11 map is a new handoff coordination plan, not evidence
   that those projects executed or that its forecast was measured.
7. P7 implementation requires complete caller inventory, offline parity and
   rollback rehearsal. Production cutover/retirement and live dependency absence
   remain deferred; current acceptance covers migration readiness only. Reigh-worker remains a
   transitional bootstrap/readiness substrate and Wan2GP/prebuild dependency;
   it is not required by the managed-result contract and is not retired.
8. Receiver model/native delegation availability, existing agent budgets and
   durable scheduler/supervisor are unverified. Provider credentials, GPU capacity
   and a GPU spend budget are not prerequisites for current project completion. A chat
   wakeup loop is not durable unattended coverage. Preserve all run counters;
   unknown or exhausted budgets do not reset during handoff.
9. Packet files are new, uncommitted content at the intended public tracked
   path. There is no packet-host commit or published packet ref. Product
   commits and archive identity are separate. No push or merge was performed.

The public retrieval report records 11 successful exact source checkouts,
one unavailable pin, and one later-added unchecked entry. Product tests were
not rerun during packaging. Exported reviews/test summaries are recorded
historical evidence; excluded raw evidence is not independently recertified.
A later CPU project completion packet needs a new seal/build after applicable
offline criteria and source/control identities are refreshed; it need not wait
for gate L. This revision changes scope only and does not assert product completion. See [recovery](recovery.md) for that order.
