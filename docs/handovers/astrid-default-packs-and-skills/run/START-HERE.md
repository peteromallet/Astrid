# Start here

Continue the existing Astrid Megado run from its scoped-complete state. Do not
restart T0–T6 or reset the recorded review/oracle counters.

1. Read `agent_goal.md`, `northstar.md`, `run.yaml`, `status.md`,
   `implementation-criteria.md`, and `tasklist.md` in this directory.
2. Read the linked plans under `docs/plans/` and the review records under
   `reviews/`.
3. Confirm the checked-out Astrid source is
   `cebd5f3b8982e785ed56973d132e8019ecc1cdbf` or a descendant that preserves
   this candidate.
4. Resume only the pending Hivemind H3/T1 boundary described in
   `agent_goal.md`. Keep the existing scoped Astrid evidence and counters.

The completed scoped result includes managed source setup, shared discovery and
host fencing, composed skill views, package navigation, generic project scope
metadata, and the reviewed Astrid test suite. The upstream Hivemind v2
manifest/release pin and live external-pack acceptance remain pending.

The original host retains consequential oracle authority. If that host is not
available, pause decisions requiring a new ruling and obtain an explicit
reassignment; do not silently replace the oracle. Ordinary work uses Luna high
and justified XHARD work uses Sol high, as recorded in `run.yaml`.
